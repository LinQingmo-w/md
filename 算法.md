## 排序算法

![常见排序](https://tva1.sinaimg.cn/large/006tNbRwly1ga0zwp6f05j30og0inmy0.jpg)



### 冒泡排序

```objective-c
//排序
//1.冒泡排序
- (NSMutableArray *)bubblingSorting:(NSMutableArray *)array {
    int i = 0;
    int j = 0;
    int n = 0;//同时找最大值的最小需要两个下标遍历
    
    BOOL isSwaped = NO;//记录是否中间已经到有序了，如果中间到有序了就不继续了
    int post = 0;//循环中y记录最后一次修改的位置
    int k = (int)array.count - 1;//给定初始值，之后= post
        
    for ( i = 0; i < array.count - 1; i ++) {
        //
        isSwaped = NO;
        post = 0;
        //正向寻找最大值
        for (j = 0; j < k ; j ++ ) {
            if ([array[j] intValue] > [array[j + 1] intValue]) {//大于了交换 是降序排序。小了交换是生序排序
                NSNumber *temp = array[j + 1];
                array[j + 1] = array[j];
                array[j] = temp;
                isSwaped = YES;
                post = j;
            }
        }
        if (!isSwaped) {
            return array;//如果没有交换，全部结束了，不是break
        }
        k = post;//下一次比较到记录位置即可
        
        
        //反向寻找最小值
        for (j = k; j > n; j--) {
            if ([array[j - 1] intValue] > [array[j] intValue]) {
                NSNumber *temp = array[j - 1];
                array[j - 1] = array[j];
                array[j] = temp;
              isSwaped = YES;
            }
        }
        n++;
        if (!isSwaped) {//如果没有交换过元素，则已经有序,直接结束
           return array;//如果没有交换，全部结束了，不是break
        }
        
    }
    return array;
}
```



```objective-c
- (void)viewDidLoad {
    [super viewDidLoad];
       
  UIButton *button1 = [[UIButton alloc] initWithFrame:CGRectMake(50, 200, 100, 100)];
    [button1 addTarget:self action:@selector(firstBtn) forControlEvents:UIControlEventTouchUpInside];
    button1.backgroundColor = [UIColor yellowColor];
    [self.view addSubview:button1];
    
    UIButton *button2 = [[UIButton alloc] initWithFrame:CGRectMake(250, 200, 100, 100)];
     [button2 addTarget:self action:@selector(secondBtton) forControlEvents:UIControlEventTouchUpInside];
     button2.backgroundColor = [UIColor purpleColor];
     [self.view addSubview:button2];
}

- (void)firstBtn {
    NSMutableArray *soreArr = @[@1,@4,@9,@5,@7,@0,@3,@8,@6,@2].mutableCopy;
    soreArr =  [self bubblingSorting:soreArr];
    NSLog(@"sourArr: %@",soreArr);
}


- (void)secondBtton {
//    NSMutableArray *soreArr = @[@1,@4,@5,@0,@3,@2,@6,@7,@8,@9].mutableCopy;
        NSMutableArray *soreArr = @[@0,@1,@2,@3,@4,@7,@6,@8,@9,@5].mutableCopy;
    soreArr =  [self bubblingSorting:soreArr];
    NSLog(@"sourArr: %@",soreArr);
}
```

